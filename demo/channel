###　管道特性 : 通过通信共享内存，不通过共享内存实现通信 （通信: 函数间交换数据）
１.在任何时间点只有一个协程操作线程安全，数据结构为队列（只能存放指定类型）
２.没有协程且有缓冲区情况下，如果管道只有写或读当超过缓冲区会造成死锁
3 .定义为非缓冲通道chan := make(chan int) 需要有接受和发送方否则会直接死锁
4.for-range遍历时如未关闭通道（只能读不能写）则也会发生死锁，关闭后会一直读取到最后空值
5.select关键字可以解决管道阻塞问题